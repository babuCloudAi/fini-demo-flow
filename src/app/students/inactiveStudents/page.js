import {Box} from '@mui/material';
import InactiveStudents from '@/components/dashboard/inactiveStudents';

export default function InactiveStudentsPage() {
    return (
        <Box component="main">
            <InactiveStudents />
        </Box>
    );
}
